name: Patch FAQ Schema

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python scripts/patch_faq_schema.py
        continue-on-error: true
      - run: |
          git config user.name "Calccover Bot"
          git config user.email "bot@calccover.com"
          if [[ -n $(git status --porcelain) ]]; then
            git add .
            git commit -m "Add FAQPage schema to calculator pages"
            git push
          fi
